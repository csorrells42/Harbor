const token='${HARBOR_ROOT}';
export const officeComponents=[
  {id:'duckdb',source:'duckdb-mcp',name:'DuckDB',repository:'https://github.com/motherduckdb/mcp-server-motherduck.git',install:['${STAGE}'],args:['-c','from mcp_server_motherduck import main; main()','--db-path',':memory:','--read-write','--allow-switch-databases'],notes:'Local DuckDB analytics with write access. Start with an in-memory database and switch to a chosen file when persistence is needed. No MotherDuck account is required for local use.'},
  {id:'markitdown',source:'markitdown',name:'MarkItDown',repository:'https://github.com/microsoft/markitdown.git',install:['${STAGE}/packages/markitdown[all]','${STAGE}/packages/markitdown-mcp'],args:['-m','markitdown_mcp'],notes:'Microsoft document-to-Markdown conversion. Local Office/PDF/HTML conversion needs no API key. Optional cloud, media transcription and OCR features have additional requirements.'},
  {id:'excel',source:'excel-mcp',name:'Excel',repository:'https://github.com/haris-musa/excel-mcp-server.git',install:['${STAGE}','mcp<2'],args:['-m','excel_mcp','stdio'],notes:'Creates and edits XLSX workbooks without Microsoft Excel. Formulas are stored but not recalculated by openpyxl; use an actual calculation engine when fresh formula results are required.'},
  {id:'word',source:'word-mcp',name:'Word Documents',repository:'https://github.com/SecurityRonin/docx-mcp.git',install:['${STAGE}','mcp<2'],args:['-c','from docx_mcp.server import main; main()'],notes:'Creates and edits DOCX files without Microsoft Word, including tables and tracked changes. Use a separate document_handle for each concurrent workflow. Starts the server directly without installing a companion Claude skill. Optional PII models are not bundled.'}
];
export function officeServer(c){return {id:c.id,name:c.name,transport:'stdio',runtime:'native',command:`${token}/runtimes/python/python.exe`,args:c.args,cwd:`${token}/data/workspace`,env:{PYTHONPATH:`${token}/support/python-site;${token}/packages/${c.id}/python`,FASTMCP_CHECK_FOR_UPDATES:'off',DO_NOT_TRACK:'1'},autoStart:false,autoRestart:true};}
export function officeRecipe(c){return {id:c.id,name:c.name,path:`packages/${c.id}`,repository:c.repository,excludeFromStage:['python'],generatedPaths:['python'],build:[{command:`${token}/runtimes/uv/uv.exe`,args:['pip','install','--python',`${token}/runtimes/python/python.exe`,'--target','${STAGE}/python',...c.install]}],verify:[{command:`${token}/runtimes/node/node.exe`,args:[`${token}/support/verify-office-tools.mjs`,token,c.id,'${STAGE}']}],notes:c.notes};}
